# Portfolio 54 Focal Adjustment — Validation Notes

Portfolio 54’s preview now uses `object-position: 60% center` instead of a hard right anchor. This shifts the visible source window left, which moves the model right inside the 54 feature tile and brings the left foot and a larger portion of the bench back into the composition.

The feature’s dimensions, Group 5 terminal geometry, 18px limestone gutter, dark Portfolio field, and native full-image viewer are unchanged. Live browser inspection of the lower Portfolio confirms the revised preview is not overlapped by adjacent tiles and the restored lower-left detail is visible.
